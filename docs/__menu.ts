import { PageMenu } from '../types';

const menu: PageMenu = {
  name: 'Documentation',
  dir: 'docs',
  pages: [
    {
      name: 'ControlBox protocol',
      path: 'reference/controlbox_spark_protocol',
    },
  ],
};

export default menu;
